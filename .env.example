# Database
DATABASE_URL=postgresql://user:password@localhost:5432/db_name

# Auth secrets
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
# Auth token expiration (e.g. 15m, 7d)
JWT_ACCESS_EXPIRES=
JWT_REFRESH_EXPIRES=

# Environment mode (development | production | test)
NODE_ENV=